class Solution {
  public:
    int isLengthEvenOrOdd(struct Node* head) {
        // Code here
        int count=0;
        while(head!=NULL)
        {
            count++;
            head=head->next;
        }
        return (count%2==0)?0:1;
    }
};
