class Solution {
  public:
    int findElementAtIndex(int n, int key, vector<int> &arr) {
        // code here
        for(int i=0;i<n-1;i++)
        {
            if(i==key)
              return arr[i];
        }
    }
};
